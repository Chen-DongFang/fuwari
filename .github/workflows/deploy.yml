name: Deploy to Server

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm build

      - name: Install sshpass
        run: sudo apt-get install -y sshpass

      - name: Deploy static files
        run: |
          sshpass -p "$SERVER_PASSWORD" rsync -avz --delete \
            -e "ssh -o StrictHostKeyChecking=no" \
            dist/ \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:/var/www/yunxing.fun/
        env:
          SERVER_PASSWORD: ${{ secrets.SERVER_PASSWORD }}

      - name: Deploy API server
        run: |
          sshpass -p "$SERVER_PASSWORD" rsync -avz \
            -e "ssh -o StrictHostKeyChecking=no" \
            api-server.js \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:/var/www/blog-api/
          sshpass -p "$SERVER_PASSWORD" rsync -avz \
            -e "ssh -o StrictHostKeyChecking=no" \
            public/data/ \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:/var/www/blog-api/data/
          sshpass -p "$SERVER_PASSWORD" rsync -avz \
            -e "ssh -o StrictHostKeyChecking=no" \
            src/data/ \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:/var/www/blog-api/src-data/
        env:
          SERVER_PASSWORD: ${{ secrets.SERVER_PASSWORD }}

      - name: Deploy data files to public
        run: |
          sshpass -p "$SERVER_PASSWORD" rsync -avz \
            -e "ssh -o StrictHostKeyChecking=no" \
            public/data/ \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:/var/www/yunxing.fun/data/
        env:
          SERVER_PASSWORD: ${{ secrets.SERVER_PASSWORD }}

      - name: Restart services
        run: |
          sshpass -p "$SERVER_PASSWORD" ssh -o StrictHostKeyChecking=no \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
            "supervisorctl restart fuwari-blog && supervisorctl restart blog-api"
        env:
          SERVER_PASSWORD: ${{ secrets.SERVER_PASSWORD }}
